# MetaArmy Environment Configuration
# 
# SECURITY NOTE: This file shows the structure for environment variables.
# Never commit actual API keys or private keys to version control.
# Set these in your production environment or local .env.local file.

# =============================================================================
# PUBLIC CONFIGURATION (Safe for frontend - prefixed with NEXT_PUBLIC_)
# =============================================================================

# MetaMask & Wallet Configuration
NEXT_PUBLIC_WALLETCONNECT_PROJECT_ID=your_walletconnect_project_id

# Smart Contract Addresses (Public by nature)
NEXT_PUBLIC_META_PLOT_AGENT_ADDRESS=your_meta_plot_agent_address
NEXT_PUBLIC_DAO_ADDRESS=your_dao_address
NEXT_PUBLIC_ARMY_TOKEN_ADDRESS=your_army_token_address
NEXT_PUBLIC_DEPLOYMENT_BLOCK=your_deployment_block

# Known Testnet Addresses (Public)
NEXT_PUBLIC_AAVE_POOL_ADDRESS=your_aave_pool_address
NEXT_PUBLIC_USDC_ADDRESS=your_usdc_address
NEXT_PUBLIC_NETWORK=sepolia

# Indexing Configuration
NEXT_PUBLIC_ENVIO_ENDPOINT=https://indexer.bigdevenergy.link/your-indexer/v1/graphql

# Analytics (Optional - safe to be public)
NEXT_PUBLIC_GA_MEASUREMENT_ID=your_google_analytics_id_here

# =============================================================================
# PRIVATE CONFIGURATION (Server-side only - NO NEXT_PUBLIC_ prefix)
# =============================================================================

# Blockchain Network Configuration
SEPOLIA_RPC_URL=https://sepolia.infura.io/v3/your_infura_project_id

# API Keys (NEVER expose these to frontend)
ETHERSCAN_API_KEY=your_etherscan_api_key
ALCHEMY_API_KEY=your_alchemy_api_key
INFURA_PROJECT_ID=your_infura_project_id

# AI/LLM Configuration
GEMINI_API_KEY=your_gemini_api_key
NEXT_PUBLIC_GEMINI_API_KEY=your_gemini_api_key_for_frontend

# ERC-7715 & Account Abstraction (EXACT helper2 pattern)
NEXT_PUBLIC_PIMLICO_API_KEY=your_pimlico_api_key

# Deployment Keys (NEVER commit to version control)
PRIVATE_KEY=your_private_key_for_deployment

# =============================================================================
# SECURITY GUIDELINES
# =============================================================================
# 
# 1. NEVER commit private keys or API keys to version control
# 2. Use NEXT_PUBLIC_ prefix ONLY for data that's safe to expose publicly
# 3. API keys should always be server-side only (no NEXT_PUBLIC_ prefix)
# 4. Set sensitive variables in production environment, not in files
# 5. Regularly rotate API keys and private keys
# 6. Use different keys for development and production environments